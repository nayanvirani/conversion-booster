[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
# Install ALL deps including devDependencies (needed for remix vite:build).
# NODE_ENV=production causes npm ci to skip devDeps, so override it here.
cmds = ["npm ci --include=dev", "npm run build"]

[start]
cmd = "npm start"
